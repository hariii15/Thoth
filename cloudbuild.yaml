steps:
  # Step 1: Build the container image using Google Cloud Buildpacks
  - name: 'gcr.io/k8s-skaffold/pack'
    args:
      - 'build'
      - 'asia-south2-docker.pkg.dev/thoth-466511/cloud-run-source-deploy/thoth:$BUILD_ID'
      - '--builder=gcr.io/buildpacks/builder:latest'
      - '--path=.'
    id: 'Build'

  # Step 2: Push the container image to Artifact Registry
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'push'
      - 'asia-south2-docker.pkg.dev/thoth-466511/cloud-run-source-deploy/thoth:$BUILD_ID'
    id: 'Push'

  # Step 3: Deploy the container image to Cloud Run
  - name: 'gcr.io/google-appengine/exec-wrapper'
    args:
      - '-i'
      - 'asia-south2-docker.pkg.dev/thoth-466511/cloud-run-source-deploy/thoth:$BUILD_ID'
      # REMOVED: The incorrect -s 'thoth:latest' flag has been deleted from here.
      - '--'
      - 'gcloud'
      - 'run'
      - 'deploy'
      - 'thoth'
      - '--platform=managed'
      - '--image=asia-south2-docker.pkg.dev/thoth-466511/cloud-run-source-deploy/thoth:$BUILD_ID'
      - '--region=asia-south2'
      - '--allow-unauthenticated'
      - '--project=thoth-466511'
    id: 'Deploy'

# Store the image in Artifact Registry
images:
  - 'asia-south2-docker.pkg.dev/thoth-466511/cloud-run-source-deploy/thoth:$BUILD_ID'

options:
  logging: CLOUD_LOGGING_ONLY
